// Package rig is the public composition root for defining an agent rig and creating or
// restoring its sessions. It owns design-time topology and lifecycle policy while the
// returned session contracts expose live runtime behavior.
package rig
